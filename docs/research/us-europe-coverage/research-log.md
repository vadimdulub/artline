# Research log: 9 September 2026

The planning tool was unavailable; `docs/us-europe-catalogue-plan.md` records phases and the gap matrix. Deep-research instructions required two substantial independent lanes: US directories/NGA schema and European national directories/Louvre. Existing workers were reused for read-only research only. The coordinator performed source verification, source downloads, backend changes, database application, tests and artifact production.

Discovery searched official art-museum directory sources, IMLS MDF/current NMS, Louvre collection counts and JSON/CSV documentation, NGA open metadata, Muséofile, Wikidata data access, current Rijksmuseum harvesting, Met CSV and Chicago bulk-dump recommendations. Worker evidence spans 12 European national source routes plus the US. The claim/source ledger retains primary URLs, publishers, dates or explicit unknown dates, confidence, native references and access limitations.

Follow-up reconciled Louvre's >500k overall scope with >10k departmental records and stale exact search counts; IMLS's old named directory with newer confidential survey data; organizations with sites; art-only scope with general museums; missing NGA date labels with artist-lifetime search bounds; current artist roles with owner/donor/history records; physical separable panels with virtual and inseparable records. The source-provided `artist` role was verified as eligible for sole current makers of classified paintings, correcting an initial overly restrictive analysis before database apply.

Operational metadata acquisition: pinned NGA Git export with six CSV tables and documentation; official Muséofile CSV; country-partitioned Wikidata classification queries. All files have URL, SHA, size and UTC retrieval manifests. Italy's two attempts failed; the failed geography remains unknown, not empty. Throttled/bounded requests and no image retrieval. Country partitions returning zero are recorded, not interpreted as absence of museums. Basic retry responses do not supply detailed closure/parent/place fields.

National directory limitations and unharvested catalogue sources are retained in the ledger/report, not silently promoted into enabled connectors. No accounts, login bypass, rate-limit circumvention or external writes were used. The user's existing 111-row source CSV passed structural validation; its previous verification labels remain source claims until rechecked.

Stop decision: the operational NGA batch and museum discovery snapshots had primary documentation, tested source access, audited gates, a backup, successful database dry-runs/applications and repeatable verification. Additional broad searches would not change those immediate import decisions. The task's exhaustive ambition remains unfinished: national census reconciliation, Italy retrieval, authority-backlog promotion, full Louvre/Rijksmuseum/Met/Chicago harvests, refresh workflows and larger load tests remain. The report presents a completed first pass, not completion of every museum or catalogue.
