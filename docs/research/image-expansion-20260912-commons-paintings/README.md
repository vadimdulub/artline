# Romney painting recovery — 13 September 2026

The painting supplement encountered retired Met API ID **437501**. Its
existing record, George Romney’s *Mrs. Bryan Cooke (Frances Puleston,
1765–1818)*, was recovered from the museum-donated
[Commons file](https://commons.wikimedia.org/wiki/File:Mrs._Bryan_Cooke_(Frances_Puleston,_1765–1818)_MET_DP169397.jpg).
The original museum XML identifies that exact object ID, title, artist,
filename, and eligible creation range ending in 1791. Both the historical
museum image flag and the current Commons file CC0 licence passed the same
identity/rights gates as the earlier three-file recovery. The original
museum reproduction was selected, not the separately colour-modified file.

The **93,483-byte** derivative was saved locally, uploaded with a GCS checksum
check, and attached in both databases before authentication expired. The
append-only event records both successful attachments. The image was visually
inspected. `verification-local.json` independently checked the local file,
rights evidence, metadata and exact artwork link with no errors. A fresh
independent cloud audit is **pending Google Cloud reauthentication**; this
local-only report does not claim to verify cloud state.

This is one deliberate overlap with a failed painting-supplement ID. Count
the recovered artwork once across campaigns; its historical HTTP 404 is no
longer an unresolved image gap. Original API captures, hashes, file revision,
rights snapshot and derivative receipt are retained.

## Cloud verification completed

`verification-final.json` now independently verifies the **93,483-byte**
Romney image in local storage, GCS, and both databases. All hashes, sizes,
metadata, rights evidence and exact artwork links passed with no errors.
This one-artwork recovery is complete.
